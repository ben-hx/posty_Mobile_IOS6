//
//  RESTManager.swift
//  posty_Mobile
//
//  Created by admin on 26.05.15.
//  Copyright (c) 2015 ben-hx. All rights reserved.
//

import Foundation

import Alamofire
import BrightFutures

public class RESTManager
{
    /*
    private struct Consts
    {
        static let AuthToken = "auth_token"
    }
    
    let manager = Alamofire.Manager.sharedInstance
    
    func handleRequest(method: Alamofire.Method, resource: String, completionHandler: (NSURLRequest, NSHTTPURLResponse?, AnyObject?, NSError?) -> Void)
    {
        let parameter: [String: String] = [
            Consts.AuthToken: APIs.sharedInstance.currentAPI.authKey,
        ]
        
        manager.request(method, APIs.sharedInstance.currentAPI.url+resource, parameters: parameter).responseJSON { (request, response, json, error) in
                completionHandler(request, response, json, error)
        }
    } 
*/

    
}